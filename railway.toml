[build]
buildCommand = "pnpm --filter @leadreacher/api build"

[deploy]
startCommand = "pnpm --filter @leadreacher/api start"
healthcheckPath = "/ready"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
