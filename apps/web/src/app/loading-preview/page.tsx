import { Loading } from "@/components/ui/Loading";

export default function LoadingPreviewPage() {
  return (
    <main className="grid min-h-dvh place-items-center overflow-hidden bg-[#362a89] p-6">
      <Loading />
    </main>
  );
}
