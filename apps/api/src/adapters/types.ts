export type UnipileCredentials = {
  dsn: string;
  apiKey: string;
};

export type UnipileProfile = {
  provider_id: string;
  public_identifier: string;
  first_name: string;
  last_name: string;
  headline: string;
};
