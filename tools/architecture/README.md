# Architecture checks

Run `pnpm test:architecture` and `pnpm check:architecture` to verify import
resolution, explicit feature public modules, platform dependency direction, and
dependency cycles. The checks are read-only and run in CI.
